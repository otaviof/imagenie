# `imagenie`

```sh
imagenie trim ...
imagenie push ...
```

```sh
imagenie reduce \
    <source-image> <runtime-image> <target-image> \
        --copy="<origin-path>[:destinatation-path]" \
        --copy="<origin-path>[:destinatation-path]"
```
